spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.datasource.url=jdbc:postgresql://localhost:5432/managecost
spring.datasource.username=managecost
spring.datasource.password=managecost

# The development environment hostname
cost-count.hostname=localhost
# allows all origins, but you may want to only allow certain ones in other environments such as production.
cost-count.cors-allowed-origins=*
