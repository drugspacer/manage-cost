import React, {FC} from "react";
import Grid from "@mui/material/Grid";
import ITrip from "../models/trip.model";
import TripCard from "./TripCard";

const TripCardList: FC<{trips: ITrip[]}> = ({trips}) => null;

export default TripCardList;